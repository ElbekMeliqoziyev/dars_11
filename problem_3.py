


"""BankUser klassini yozing.
1. register(), login(), deposit(), withdraw(), logout() metodlari bo‘lsin.
	Foydalanuvchilar JSON faylga saqlansin.

2. Har bir foydalanuvchida username, password, balance, is_authenticated maydonlari bo‘lsin.

3. Agar is_authenticated=False bo‘lsa, foydalanuvchi faqat login() qila olsin.

4. deposit() va withdraw() faqat login qilinganlarda ishlasin."""

# class User:
#     def __init__(self):
        

# class BankUsers:
#     def __init__(self, username, password, balance, is_authenticated):
#         self.username = username
#         self.password = password
#         self.balance = balance
#         self.is_authenticated = is_authenticated

#     def register(self):
